import React from "react";
import SectionContainer from "~/components/shared/SectionContainer";
import TeamSelectionPage from "~/container/TeamSelection/TeamSelectionPage";

const TeamSelection = () => {
  return (
    <SectionContainer>
      <TeamSelectionPage />
    </SectionContainer>
  );
};

export default TeamSelection;
